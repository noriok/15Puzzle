using UnityEngine;

static class Util {
    // public static T Bless<T>(UnityEngine.Object obj, Vector3 pos) {
    //     var g = (GameObject)GameObject.Instantiate(obj, pos, Quaternion.identity);
    //     return g.GetComponent<T>();
    // }
}
